name: Deploy VitePress site to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

# 设置权限
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 确保获取完整历史
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18  # 更新到新版本的Node
          cache: 'npm'
      
      - name: List workspace
        run: |
          echo "列出主要目录结构："
          ls -la
          echo "列出组件目录内容："
          ls -la docs/.vitepress/theme/components/ || echo "目录不存在"
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2  # 更新到 v2 版本
        with:
          path: docs/.vitepress/dist

  deploy:
    # 标准的 GitHub Pages 环境
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2 